{
  "name": "@supreme/api",
  "version": "1.0.0",
  "private": true,
  "description": "Supreme API - Professional MVC Express Application for Vercel Serverless",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p build && echo 'Build successful' > build/.build",
    "lint": "echo 'Lint not configured yet'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf .turbo node_modules"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.x"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.19.2",
    "mongoose": "^8.3.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "turbo": {
    "outputs": []
  }
}